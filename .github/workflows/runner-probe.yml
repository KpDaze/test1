name: Runner probe

on:
  push:
    paths:
      - '.github/workflows/runner-probe.yml'
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Echo
        run: echo "runner-ok"
